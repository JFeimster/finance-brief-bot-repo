# Codex Tasks

## Suggested Tasks

1. Add `tests/expected_multi_account_brief.md`.
2. Add `samples/sample_missing_balance_week.csv`.
3. Add a static FAQ page under `site/pages/faq.html`.
4. Improve `site/index.html` with a clearer call-to-action.
5. Add a JSON schema for normalized transactions under `actions/api/`.
6. Add prompt examples for monthly reviews under `prompts/chatgpt/`.
7. Validate all JSON files.
8. Review Markdown files for consistent headings.
