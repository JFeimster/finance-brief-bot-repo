# Jules Tasks

## Good First Tasks

- Add a site FAQ page.
- Add a multi-account expected output file.
- Add a missing-balance sample CSV.
- Improve `knowledge/gpt_knowledge_index.md`.
- Add a new prompt template under `prompts/coding_agents/`.
- Expand `actions/api/openapi.yaml` with placeholder CSV validation endpoints.

## Medium Tasks

- Create a simple sample brief viewer for the static site.
- Add a JSON schema for normalized transactions.
- Add a site changelog page that reflects `CHANGELOG.md`.
- Improve consistency across all Markdown docs.

## Review Checklist

- Files are in the correct folders.
- No real financial data was added.
- Markdown is readable.
- JSON is valid.
- The required Weekly Finance Brief format was preserved.
