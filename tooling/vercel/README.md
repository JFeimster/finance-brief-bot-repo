# Vercel Tooling

This folder contains notes for deploying or expanding the static site.

The current `site/` folder is intentionally simple:

- `index.html`
- `styles.css`
- `script.js`
- `pages/`
- `embeds/`
- `widgets/`
- `data/`

Do not convert the site to a framework unless specifically requested.
